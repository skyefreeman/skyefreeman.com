# Test Skill
1. Run `bin/rails test` to execute the full test suite
2. If any tests fail, read the failing test file and the corresponding source file
3. Fix the issue and re-run only the failing test file
4. Once passing, run the full suite again to confirm no regressions
